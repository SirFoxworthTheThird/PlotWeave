# PlotWeave uses no server-side services and requires no environment variables by default.
# Cloud sync works via the browser's File System Access API (Chrome/Edge) — users choose a
# local folder (e.g. their Google Drive or OneDrive sync folder) and PlotWeave reads/writes
# .pwk files there directly, with no OAuth or account registration required.
